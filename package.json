{
  "name": "flappy-bird",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -rf dist && webpack && tailwindcss -i ./src/styles.css -o ./dist/styles.css && mkdir dist/assets && cp -r src/assets dist"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "d3": "^7.8.2",
    "tailwindcss": "^3.2.7",
    "tippy.js": "^6.3.7",
    "webpack": "^5.75.0"
  },
  "devDependencies": {
    "webpack-cli": "^5.0.1"
  }
}
